package com.psicogest.psicogest.model.enums;

public enum SubscriptionChargeAttemptStatus {
    PENDING,
    PROCESSING,
    AWAITING_PROVIDER,
    CONFIRMED,
    FAILED,
    RECONCILIATION_REQUIRED,
    CANCELLED
}
