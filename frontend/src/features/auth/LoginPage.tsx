import { useState } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { ArrowRight, Eye, EyeOff, LockKeyhole, ShieldCheck } from "lucide-react";
import { useLocation, useNavigate } from "react-router-dom";
import { Button, Card, Input, Notice } from "../../shared/components/ui";
import { loginSchema, type LoginValues } from "../../shared/lib/validators";
import { useAuth } from "./AuthContext";

export function LoginPage() {
  const { login } = useAuth();
  const navigate = useNavigate();
  const location = useLocation();
  const [showPassword, setShowPassword] = useState(false);
  const [formError, setFormError] = useState("");
  const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm<LoginValues>({ resolver: zodResolver(loginSchema), defaultValues: { email: "demo@psicogest.com", password: "demo123" } });
  async function onSubmit(values: LoginValues) { try { setFormError(""); await login(values.email, values.password); navigate("/mfa", { state: { from: location.state?.from ?? "/" } }); } catch (error) { setFormError(error instanceof Error ? error.message : "Não foi possível entrar."); } }
  return <main className="grid min-h-screen bg-cream lg:grid-cols-[1.1fr_0.9fr] dark:bg-slate-950"><section className="relative hidden overflow-hidden bg-ink p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-36 -top-36 h-96 w-96 rounded-full border-[48px] border-sage-700/30" /><div className="absolute -bottom-48 -left-28 h-96 w-96 rounded-full border-[72px] border-coral/20" /><div className="relative flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-sage-500 font-display text-xl font-extrabold">P</span><span className="font-display text-xl font-extrabold">PsicoGest</span></div><div className="relative max-w-xl"><p className="mb-5 text-xs font-bold uppercase tracking-[0.2em] text-sage-300">Operação clínica com clareza</p><h1 className="font-display text-5xl font-extrabold leading-[1.08] tracking-tight">Cuidar da clínica também é cuidar dos dados.</h1><p className="mt-6 max-w-lg text-lg leading-8 text-slate-300">Uma visão segura para agenda, pacientes, prontuários e financeiro — respeitando o contexto de cada equipe.</p><div className="mt-10 flex flex-wrap gap-3 text-sm text-slate-300"><span className="rounded-full border border-white/15 px-3 py-2">LGPD by design</span><span className="rounded-full border border-white/15 px-3 py-2">RBAC contextual</span><span className="rounded-full border border-white/15 px-3 py-2">Auditoria nativa</span></div></div><p className="relative text-xs text-slate-500">v0.9 · Ambiente de demonstração</p></section><section className="flex items-center justify-center p-5 sm:p-10"><Card className="w-full max-w-md border-0 p-7 shadow-xl sm:p-9 dark:border dark:border-slate-800"><div className="mb-8 lg:hidden"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-sage-600 font-display text-lg font-extrabold text-white">P</span><span className="font-display text-xl font-extrabold text-ink dark:text-white">PsicoGest</span></div></div><div className="mb-8"><p className="text-xs font-bold uppercase tracking-[0.18em] text-sage-600">Bem-vinda de volta</p><h2 className="mt-2 font-display text-3xl font-extrabold tracking-tight text-ink dark:text-white">Acesse sua clínica</h2><p className="mt-2 text-sm leading-6 text-slate-500">Use suas credenciais para continuar na Clínica Horizonte.</p></div><form className="space-y-5" onSubmit={handleSubmit(onSubmit)} noValidate><Input label="E-mail profissional" type="email" autoComplete="email" placeholder="voce@clinica.com" error={errors.email?.message} {...register("email")} /><div className="relative"><Input label="Senha" type={showPassword ? "text" : "password"} autoComplete="current-password" error={errors.password?.message} {...register("password")} /><button type="button" aria-label={showPassword ? "Ocultar senha" : "Mostrar senha"} className="absolute right-3 top-9 text-slate-400" onClick={() => setShowPassword((value) => !value)}>{showPassword ? <EyeOff size={18} /> : <Eye size={18} />}</button></div>{formError && <p className="rounded-xl bg-red-50 p-3 text-sm font-medium text-red-700" role="alert">{formError}</p>}<Button className="w-full" type="submit" disabled={isSubmitting}>{isSubmitting ? "Validando…" : "Continuar"}<ArrowRight size={17} /></Button></form><div className="my-6 flex items-center gap-3 text-xs text-slate-400"><span className="h-px flex-1 bg-slate-200 dark:bg-slate-800" /> ambiente seguro <span className="h-px flex-1 bg-slate-200 dark:bg-slate-800" /></div><Notice title="Acesso de demonstração"><span className="font-mono text-xs">demo@psicogest.com</span> · senha <span className="font-mono text-xs">demo123</span>. O segundo fator usa qualquer código de 6 dígitos.</Notice><div className="mt-5 flex items-center justify-center gap-2 text-xs text-slate-400"><LockKeyhole size={14} /> Sessão protegida <ShieldCheck size={14} /></div></Card></section></main>;
}
