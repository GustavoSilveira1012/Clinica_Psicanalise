import { useState } from "react";
import { Laptop2, LockKeyhole, LogOut, ShieldCheck, UserRound } from "lucide-react";
import { useAuth } from "../auth/AuthContext";
import { roleLabels } from "../../shared/lib/permissions";
import { Button, Card, Input, Notice, PageHeader, StatusBadge } from "../../shared/components/ui";
import { useToast } from "../../shared/providers/ToastProvider";

export function ProfilePage() {
  const { session } = useAuth();
  const [active, setActive] = useState<"profile" | "security" | "sessions">("profile");
  const toast = useToast();
  if (!session) return null;
  return <div><PageHeader eyebrow="Minha conta" title="Perfil e segurança" description="Dados da conta, segundo fator e sessões ativas." /><div className="grid gap-6 lg:grid-cols-[230px_1fr]"><Card className="h-fit p-2"><button onClick={() => setActive("profile")} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-bold ${active === "profile" ? "bg-sage-50 text-sage-700 dark:bg-sage-950/40 dark:text-sage-200" : "text-slate-500"}`}><UserRound size={16} /> Perfil</button><button onClick={() => setActive("security")} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-bold ${active === "security" ? "bg-sage-50 text-sage-700 dark:bg-sage-950/40 dark:text-sage-200" : "text-slate-500"}`}><ShieldCheck size={16} /> Segurança</button><button onClick={() => setActive("sessions")} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-bold ${active === "sessions" ? "bg-sage-50 text-sage-700 dark:bg-sage-950/40 dark:text-sage-200" : "text-slate-500"}`}><Laptop2 size={16} /> Sessões</button></Card><Card className="p-6">{active === "profile" && <div><div className="flex items-center gap-4"><span className="grid h-16 w-16 place-items-center rounded-3xl bg-sage-100 font-display text-2xl font-extrabold text-sage-700 dark:bg-sage-900 dark:text-sage-200">{session.user.initials}</span><div><h2 className="font-display text-xl font-extrabold text-ink dark:text-white">{session.user.name}</h2><p className="mt-1 text-sm text-slate-500">{roleLabels[session.user.role]} · {session.user.tenant.name}</p></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><Input label="Nome" defaultValue={session.user.name} /><Input label="E-mail" defaultValue="he•••@clinicahorizonte.com" disabled /></div><Button className="mt-5" onClick={() => toast("Perfil atualizado com auditoria.")}>Salvar perfil</Button></div>}{active === "security" && <div><ProfileTitle icon={<LockKeyhole />} title="Segurança da conta" description="MFA ativo e credenciais protegidas." /><div className="mt-6 space-y-4"><Notice title="MFA habilitado" tone="success">Sua conta exige segundo fator para entrar. A recuperação deve ser feita com códigos de uso único.</Notice><div className="flex items-center justify-between rounded-2xl border border-slate-100 p-4 dark:border-slate-800"><div><p className="text-sm font-bold text-ink dark:text-white">Aplicativo autenticador</p><p className="mt-1 text-xs text-slate-500">Configurado em 14 ago 2026 · último uso há 2 min</p></div><StatusBadge value="ACTIVE" /></div><Button variant="secondary" onClick={() => toast("Fluxo de rotação de MFA iniciado.")}><ShieldCheck size={15} /> Reconfigurar MFA</Button></div></div>}{active === "sessions" && <div><ProfileTitle icon={<Laptop2 />} title="Sessões ativas" description="Revogue acessos que você não reconhece." /><div className="mt-6 space-y-3"><Session name="Este dispositivo" description="Windows · Chrome · São Paulo" current /><Session name="iPhone 15" description="iOS · último acesso ontem" /><Session name="MacBook Pro" description="macOS · último acesso 02 set 2026" /></div><Button variant="danger" className="mt-5" onClick={() => { toast("Todas as outras sessões foram revogadas."); }}><LogOut size={15} /> Revogar outras sessões</Button></div>}</Card></div></div>;
}
function ProfileTitle({ icon, title, description }: { icon: React.ReactNode; title: string; description: string }) { return <div className="flex items-start gap-3"><div className="rounded-xl bg-sage-50 p-2.5 text-sage-700 dark:bg-sage-950/40 dark:text-sage-300">{icon}</div><div><h2 className="font-display text-lg font-bold text-ink dark:text-white">{title}</h2><p className="mt-1 text-sm text-slate-500">{description}</p></div></div>; }
function Session({ name, description, current = false }: { name: string; description: string; current?: boolean }) { return <div className="flex items-center justify-between rounded-2xl border border-slate-100 p-4 dark:border-slate-800"><div className="flex items-center gap-3"><Laptop2 className="text-slate-400" size={19} /><div><p className="text-sm font-bold text-ink dark:text-white">{name}</p><p className="mt-1 text-xs text-slate-500">{description}</p></div></div>{current ? <span className="text-xs font-bold text-emerald-600">Atual</span> : <button className="text-xs font-bold text-red-600 hover:underline">Revogar</button>}</div>; }
